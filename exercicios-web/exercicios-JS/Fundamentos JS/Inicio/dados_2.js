// LET IDENTIFICADOR = VALOR
let idade = 3;
//usar identificadores com nomes claros e objetivos é essencial para o código ficar mais legível.

let preco = 19.90;
let desconto = 0.4;
let precoComDesconto = preco * (1 - desconto);
console.log(precoComDesconto); 

let nome = "Caderno"
let categoria = "Papelaria"
console.log("Nome: " + nome + ", Categoria: " + categoria + ", Preço: " + preco + ", Desconto: " + desconto + ", Preço com desconto: " + precoComDesconto);