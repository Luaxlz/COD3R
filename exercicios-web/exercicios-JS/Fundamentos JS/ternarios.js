const resultado = nota => nota >= 7 ? "Aprovado" : "Reprovado" // função Arrow com operador ternário ? que basicamente funciona como um if else, se for verdadeiro retorna algo se for falso retorna outro algo.

console.log(resultado(7.1))
console.log(resultado(6.1))
